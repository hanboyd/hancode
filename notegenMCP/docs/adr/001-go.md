# ADR 001：Go 与 MCP SDK

状态：接受。

使用 Go 1.26.5 和官方 `github.com/modelcontextprotocol/go-sdk` v1.6.1。该 SDK 是 MCP 官方 Tier 1 Go SDK，提供 STDIO、tool schema/annotations、context cancellation 和结构化结果。业务层不暴露 SDK 类型，避免未来传输升级牵动存储实现。
